[project]
name = "bunnybook-kimi-runtime"
version = "0.1.0"
description = "Read-only MCP runtime bundled with the BunnyBook Kimi Code test plugin"
requires-python = ">=3.11"
dependencies = [
  "mcp[cli]>=1.27,<2",
]

[tool.uv]
package = false

